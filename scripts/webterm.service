[Unit]
Description=WebTerm — Web-based SSH/SFTP/DB terminal manager
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/webterm -config /etc/webterm/config.yaml
Restart=on-failure
RestartSec=5
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
